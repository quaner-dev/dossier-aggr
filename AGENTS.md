# AGENTS

本文件是仓库公开的 AI/代码代理协作说明。详细上下文、讨论记录和原始协议资料仅保存在本地，不纳入 Git。

## 项目基线

- 技术栈：FastAPI、SQLModel、PostgreSQL、Alembic。
- `main.py` 是 FastAPI 应用入口；仓库交付 API 服务和数据库迁移，不包含前端应用。
- `api/` 负责 HTTP 路由和协议包装，`services/` 负责业务编排，`repo/` 负责数据访问，`models/` 负责协议模型和表模型。
- `core/` 保存配置、数据库、认证、异常和通用工具，`alembic/` 保存数据库迁移，`tests/` 保存分层及协议测试。
- `docs/data-push-architecture.md` 是数据接入与推送边界的公开基线；修改相关边界前应同步更新设计文档。

## 分层与事务

- `api/` 只处理 HTTP 参数、依赖注入、协议包装和响应，不直接访问数据库或内联业务逻辑。
- `services/` 组织业务流程；所有查询和写入均通过 `repo/`，不要为普通数据库写入重新引入后台任务或消息队列。
- `repo/` 使用异步 SQLModel/SQLAlchemy 会话完成过滤、排序、分页和持久化，不处理 HTTP 响应。
- 写接口只能在事务提交成功后返回成功；批量操作应保持单次请求的事务一致性，避免部分提交。
- 保持现有异常边界；除非协议明确要求变化，HTTP 错误继续使用 `ResponseStatusListObject` 协议包装。

## 数据接入与推送边界

- 当前仓库只提供 FastAPI、Service、Repository 到 PostgreSQL 的同步事务路径，不为普通数据库写入引入后台任务或消息队列。
- 所有写接口只能在 PostgreSQL 事务提交成功后返回协议成功；批量操作必须保持单次请求的事务一致性，避免部分提交。
- `/VIID/SubscribeNotifications` 是入站通知接口；出站订阅推送尚未实现，不能在没有架构决策和契约测试的情况下增加异步推送组件。
- 未来若引入高并发接入或出站推送，必须先定义事件顺序、批次幂等、失败重试和连接池/并发边界，并同步更新 `docs/data-push-architecture.md`。

## 协议变更

- 本项目实现 GA/T 1400 和 GA/T 2350.5。涉及协议的修改应先核验本地 `.protocol/` 原文；若原文不可用，应说明判断依据和未确认项。
- 本地 `.ai/` 若存在，可用作协议索引和项目上下文，但它不是公开仓库的一部分，也不能替代 `.protocol/` 原文。
- 同时检查 HTTP 方法和路径、查询参数及别名、请求/响应外层对象、字段名称和大小写、必填性、枚举、日期时间格式及状态对象。
- 不以当前代码行为覆盖协议原意。兼容既有非标准行为时，应保留明确的协议主路径，并用测试记录兼容范围。

## 模型与迁移

- 数据库 schema 变化必须提供 Alembic 迁移，并补充模型和迁移测试。
- 新增表模型时，确保 Alembic 加载 `models` 后能在 `SQLModel.metadata` 中发现它；按现有导出方式同步更新 `models/__init__.py`。
- 审查迁移的 `upgrade()` 和 `downgrade()`，不要在需求未明确时改写已经发布的历史迁移。
- 应用代码使用异步 PostgreSQL 驱动；Alembic 通过项目现有的同步 URL 转换执行迁移，不要引入第二套数据库配置。

## 测试与验证

- 先运行与改动最接近的测试：API 使用 `tests/test_api_*.py`，协议契约使用 `tests/protocol/`，业务层使用 `tests/services/`，数据访问使用 `tests/repositories/`，模型和迁移分别使用 `tests/models/`、`tests/migrations/`。
- 接口行为变化时覆盖成功、参数错误和资源不存在等响应；协议字段变化时同时断言序列化后的 JSON 外形。
- 完成修改后至少尝试运行 `git diff --check`、`ruff check .`、`pyright` 和 `pytest -q`；无法执行时明确列出未执行命令和原因。

## 改动范围与仓库卫生

- 控制改动范围，避免无关重构、批量格式化和新增依赖；不要覆盖或回退工作树中与任务无关的现有改动。
- 不提交 secrets、`.env*`、本地配置、AI 详细上下文、协议原文、缓存、数据库、日志或运行产物。
- 不要假设公开克隆中存在 `.ai/` 或 `.protocol/`，也不要为了让说明成立而将它们加入 Git。
