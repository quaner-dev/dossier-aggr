# GA/T 1400 Notes

若本文与本地 `.protocol/` 原始协议冲突，以 `.protocol/` 为准。

## 覆盖范围

当前仓库覆盖：

- 系统注册、注销、保活、系统时间。
- 采集系统 APS、采集设备 APE。
- 人员 Person、人脸 Face。
- 订阅 Subscribe、订阅通知 SubscribeNotification。

## 阅读入口

- `source-files.md`：原始协议文件位置。
- `interface-index.md`：接口条款、路径和方法。
- `implementation-map.md`：接口到代码层的映射。
- `model-index.md`：协议对象到模型的映射。
- `testing-map.md`：相关测试入口。
- `known-gaps.md`：已知约束和差异。
