# AGENTS

本文件是仓库根目录的 AI/代码代理入口。完整执行规范集中在 `.ai/`。

## 阅读顺序

1. 先读 `.ai/README.md`，确认项目上下文和文档索引。
2. 再读 `.ai/AGENTS.md`，执行仓库级代理约束。
3. 涉及协议时，先看 `.ai/protocols/` 中的索引，再回到本地 `.protocol/` 原始文件核验。

## 基本约束

- 不提交 secrets、本地配置、缓存、数据库文件或运行产物。
- `.protocol/` 是本地原始协议资料目录，已被 `.gitignore` 忽略。
- 变更接口契约、架构分层或测试流程时，同步更新 `.ai/` 下对应文档。
