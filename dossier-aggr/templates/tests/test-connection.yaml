apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dossier-aggr.fullname" . }}-test-connection"
  labels:
    {{- include "dossier-aggr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dossier-aggr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
